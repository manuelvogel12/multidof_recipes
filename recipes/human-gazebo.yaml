clone:
  type: git
  server: github.com
  repository: JakobThumm/human-gazebo.git
  tag: main
  ros_src: true
  recursive: true

build:
  type: custom
  cmd:
    - echo "export ROBOT_RL_SIM_ROOT={srcdir}/../.." >> /home/user/.bashrc
