depends:
  - concert_msgs
  - human-gazebo

build:
  type: custom
  cmd:
    - rosdep update && rosdep install --from-paths src --ignore-src -r -y
    - catkin init && catkin config --install
    - catkin build